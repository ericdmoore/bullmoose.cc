{
  "name": "@bullmoose/cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "bullmoose — JMAP sync client with a local SQLite message log",
  "bin": {
    "bullmoose": "./bin/bullmoose.mjs"
  },
  "man": ["./man/bullmoose.1"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "gen:docs": "npm run build && mkdir -p man && node bin/bullmoose.mjs help --man > man/bullmoose.1 && node bin/bullmoose.mjs help --markdown > ../../docs/cli.md"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
