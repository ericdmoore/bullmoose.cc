{
  "name": "@bullmoose/cli",
  "version": "0.0.1",
  "private": true,
  "description": "bullmoose — JMAP sync client with a local SQLite message log",
  "man": [
    "./man/bullmoose.1"
  ],
  "bin": {
    "bullmoose": "./bin/bullmoose.mjs"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "gen:docs": "npm run build && mkdir -p man && node bin/bullmoose.mjs help --man > man/bullmoose.1 && node bin/bullmoose.mjs help --markdown > ../../docs/cli.md && node tools/gen-help-artifact.mjs",
    "smoke": "node smoke/contract.mjs"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
