# bouncer@ analytics queries

Deferred from the s12 boundary design (2026-08-13). Third conversation category with
bouncer@: aggregate questions — "what is our rejection rate, trailing 30d?", top rejected
domains, graduation velocity. The per-domain daily counters (s12, industrial tier) are
the substrate; the MCP analytics surface (`spend_by_month` et al.) is the pattern to
extend. Build after the first two conversations (FN report, FP rescue) work.
