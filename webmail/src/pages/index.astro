---
// ⚠️ PLACEHOLDER — s07 **T0** replaces this page entirely.
//
// `/` is meant to be the actual product: *Looking Ahead* + *Waiting Approvals*,
// the two stacks that answer "what is about to happen and what needs me". T0 is
// the task that makes this a collaboration space for people and agents rather
// than a file manager with a notifications tray, and it is the thing the eight
// sections are arranged around — not a dashboard of counts, and never a section
// of its own.
//
// It cannot render yet: it needs proposals, and nothing produces an
// `ActionProposal` until s07 T4. So until then `/` only routes — `/mail` with a
// session, `/login` without one. Replace the island; do not grow a home page
// out of it.
//
// No chrome: this document is on screen for one frame, and rendering the nav
// here would flash a full app bar and then throw it away.
import HomeRedirect from "../components/HomeRedirect.tsx";
import App from "../layouts/App.astro";
---

<App title="bullmoose" chrome={false}>
  <HomeRedirect client:only="preact" />
</App>
