---
// Placeholder route for the webmail floor (s03.C · T1). NOT a mail client yet —
// mail views (T2) and Files (T3) are deferred. This page exists to prove the
// Astro + Preact island shell builds clean and mounts.
import AppShell from "../components/AppShell.tsx";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>bullmoose webmail</title>
    <meta name="robots" content="noindex" />
  </head>
  <body>
    <!-- SPA-in-an-island (arch.md §1). client:only — the shell is
         authenticated + interactive, so it runs in the browser, not at build. -->
    <AppShell client:only="preact" />
  </body>
</html>
