{
  "name": "webmail",
  "type": "module",
  "private": true,
  "description": "bullmoose webmail — Astro shell + Preact islands over the injected JmapClient (s03.C)",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@astrojs/preact": "^6.0.1",
    "astro": "^7",
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "@bullmoose/jmap-core": "*"
  }
}
